.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
.env.*
!.env.example

web/node_modules/
web/dist/
*.tsbuildinfo

# Footage sits outside the build context, so it was never a candidate; the
# only thing worth excluding here is the crops, which are generated.
api/static/faces/*.png

.git/
tests/
notes/
*.md
!README.md
